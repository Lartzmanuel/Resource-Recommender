<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= locals.title %></title>
    <meta name="description" content="<%= locals.description %>">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@200;400;600;800;900&display=swap" rel="stylesheet">
    <script type="text/javascript" defer src="/js/script.js"></script>
    <script type="text/javascript" defer src="/js/recommender.js"></script>
</head>
<body>
  
    <% if (!locals.isPreferencePage && !locals.isloginPage) { %>
        <%- include('../partials/header.ejs') %>
    <% } %>
    
    <main>
        <%- body %>
    </main>
    
    <% if (!locals.isPreferencePage && !locals.isloginPage) { %>
        <%- include('../partials/footer.ejs') %>
    <% } %>
    
</body>
</html>